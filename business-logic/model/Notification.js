class Notification {
    constructor(title, body, icon) {
        this.notification = {
            title: title,
            body: body,
            icon: icon
        }
    }
}

module.exports = Notification;